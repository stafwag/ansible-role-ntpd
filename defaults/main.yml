---
# defaults file for ntpd

_ntpd:
  {}

ntpd_defaults:
  servers:
    - 0.be.pool.ntp.org
    - 1.be.pool.ntp.org
    - 2.be.pool.ntp.org
    - 3.be.pool.ntp.org
  service:
    ntpd
  template:
    ntp.conf.j2
  ntpd_conf:
    /etc/ntp.conf
  driftfile:
    /var/lib/ntp/drift
